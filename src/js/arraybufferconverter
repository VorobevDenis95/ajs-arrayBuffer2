import getBuffer from "./arrraybuffer";

export default class ArrayBufferConverter {
    load(buffer) {
        const uint16 = new Uint16Array(buffer);
        const str = [];
        uint16.forEach((element) => {
            str.push(String.fromCharCode(element));
        });
        return str.join('');
    }

}